import { notify } from "./notify"
import { readJson, saveJson } from "./json"
import { getFormattedData } from "./text"
import { getDeviceScreenSize, setAppSize } from "./screen-size"
import { getErrorText } from "./error"

export {
    notify,
    readJson,
    saveJson,
    setAppSize,
    getErrorText,
    getFormattedData,
    getDeviceScreenSize,
}