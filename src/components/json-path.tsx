// types
import type { FC } from "../types"

const JsonCustomPathMaker: FC = () => {
    return (
        <div>
            <h2>Custom JSON Path Maker</h2>
            <p>This is a placeholder for the custom JSON path maker component.</p>
        </div>
    )
}

export default JsonCustomPathMaker
